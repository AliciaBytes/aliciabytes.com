---
const { src, class: className, ...rest } = Astro.props;

const svg = await fetch(src);
---

<div set:html={svg} class={className} {...rest} />

<style>
  @layer component {
    div,
    div :global(svg) {
      max-width: 100%;
    }
  }
</style>
