---
import { InlineSpoiler as SolidInlineSpoiler } from "./solid/InlineSpoiler";
const { ...props } = Astro.props;
---

<!-- htmlmin:ignore -->
<SolidInlineSpoiler client:load {...props}><slot /></SolidInlineSpoiler>
<!-- htmlmin:ignore -->
