---
const { src, class: className, ...rest } = Astro.props;

const response = await fetch(src);
const armored_key = await response.text();
---

<pre set:html={armored_key} class={className} {...rest} />
